import React from 'react';
import {Container} from 'reactstrap';

const UserView = (props) => {
    const {errors} = props;
    return(
        <Container>
            <h4>User Details</h4>
        </Container>
    )

}

export default UserView;